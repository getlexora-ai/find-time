import { WaitlistScreen } from '@/landing/WaitlistScreen';

/** /waitlist — the dedicated early-access page (src/landing/WaitlistScreen.tsx).
 *  Web-only marketing surface; native never links here. */
export default function Waitlist() {
  return <WaitlistScreen />;
}
